name: Smart Quote CI/CD

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: '9.x'
  SOLUTION_PATH: 'smartquote.sln'
  API_PROJECT_PATH: 'smartquote.api'
  PUBLISH_DIR: './publish'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore ${{ env.SOLUTION_PATH }}

      - name: Build
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration Release --no-restore

      - name: Test
        run: dotnet test ${{ env.SOLUTION_PATH }} --configuration Release --no-restore --no-build --verbosity normal

      - name: Publish
        run: dotnet publish ${{ env.API_PROJECT_PATH }} --configuration Release --no-restore --no-build -o ${{ env.PUBLISH_DIR }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: webapi
          path: ${{ env.PUBLISH_DIR }}
  
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    needs: [build-and-test]
    container: ghcr.io/railwayapp/cli:latest
    env:
     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
     SVC_ID: ${{ secrets.SVC_ID }}
     JwtSettings__SecretKey: ${{ secrets.JWT_SECRET_KEY }}
     JwtSettings__Issuer: ${{ secrets.JWT_ISSUER }}
     JwtSettings__Audience: ${{ secrets.JWT_AUDIENCE }}
     JwtSettings__ExpirationMinutes: ${{ secrets.JWT_EXPIRATION_MINUTES }}
     ConnectionStrings__DefaultConnection: ${{ secrets.DB_CONNECTION_STRING }}
    steps:
    - uses: actions/download-artifact@v4
      with:
       name: webapi
       path: ./publish

    - run: railway up ./publish --service ${{ env.SVC_ID }}
